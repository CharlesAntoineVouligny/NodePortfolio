extends layout

block content
  h1= 'Results for "' + searchkey + '"'

  ul
    each book in book_list
      li 
        a(href=book.url) #{book.title}
        |  (#{book.author.name})
  

    else
      li Have you asked Dave about it?